<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>Messages</title>
  </head>
  <body>
    <%- include("navbar.ejs") %>
    <div class="flex flex-col gap-4">
      <% messages.forEach((msg) => { %>
      <%- include("msgCard.ejs", {user, msg}) %>
      <% }) %>
    </div>
  </body>
</html>
